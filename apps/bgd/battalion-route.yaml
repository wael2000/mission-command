apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: $(params.battalion)
  name: $(params.battalion)
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: $(params.battalion)
    weight: 100
